// A smart page that will be used for Nearby, Explore, and Account. This will be a reusable component since these pages will look similar.
import React from "react";

export default function EventListPage() {
    return (
        <h1>EventListPage</h1>
    );
}